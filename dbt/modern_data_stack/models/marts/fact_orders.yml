version: 2

models:
  - name: orders_stg
    columns: 
      - name: 'order_number'
        quote: true
        tests:
          - not_null
      # This creates a failing test as an example
      # - name: html_description
      #   tests:
      #     - not_null