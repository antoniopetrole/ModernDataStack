version: 2

models:
  - name: products_merge
    columns: 
      - name: 'product_code'
        tests:
          - unique
          - not_null
      # This creates a failing test as an example
      # - name: htmldescription
      #   tests:
      #     - not_null